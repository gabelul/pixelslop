# Internal planning docs — not for public consumption
dev_docs/

# macOS resource forks (external volumes create these)
._*
.DS_Store

# Dependencies
node_modules/

# Build output (dist/ is tracked — it's package content, not compiled output)
build/

# Scanner output (screenshots, reports — generated per-run)
.deslop/
.playwright-mcp/

# Pixelslop session state (generated per-run, project-local)
.pixelslop/
.pixelslop-session.log
.pixelslop-plan.md

# Local installs (created by `npx pixelslop install --project`)
.claude/
.mcp.json

# Other runtimes / tools
.opencode/
.superset/

# Environment
.env
.env.*
